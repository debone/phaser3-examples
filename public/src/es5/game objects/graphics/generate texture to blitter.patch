Index: ../public/src/es5/game objects/graphics/generate texture to blitter.new
===================================================================
--- ../public/src/es5/game objects/graphics/generate texture to blitter.new
+++ ../public/src/es5/game objects/graphics/generate texture to blitter.new
@@ -68,9 +68,9 @@
     for (var i = 0; i < 100; ++i)
     {
         launch();
     }
-
+
     updateDigits();

 }

@@ -151,9 +151,9 @@
     graphics.lineStyle(2, lineColor, 1.0);
     graphics.fillStyle(color, 1.0);
     graphics.beginPath();
     graphics.moveTo(cx, cy - outerRadius);
-    for (i = 0; i < spikes; i++) {
+    for (var i = 0; i < spikes; i++) {
         x = cx + Math.cos(rot) * outerRadius;
         y = cy + Math.sin(rot) * outerRadius;
         graphics.lineTo(x, y);
         rot += step;
