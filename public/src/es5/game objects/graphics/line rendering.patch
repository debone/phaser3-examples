Index: ../public/src/es5/game objects/graphics/line rendering.new
===================================================================
--- ../public/src/es5/game objects/graphics/line rendering.new
+++ ../public/src/es5/game objects/graphics/line rendering.new
@@ -49,9 +49,9 @@
     graphics.lineStyle(5, lineColor, 1.0);
     graphics.fillStyle(color, 1.0);
     graphics.beginPath();
     graphics.moveTo(cx, cy - outerRadius);
-    for (i = 0; i < spikes; i++) {
+    for (var i = 0; i < spikes; i++) {
         x = cx + Math.cos(rot) * outerRadius;
         y = cy + Math.sin(rot) * outerRadius;
         graphics.lineTo(x, y);
         rot += step;
