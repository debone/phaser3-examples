Index: ../public/src/es5/game objects/render texture/fireball.js
===================================================================
--- ../public/src/es5/game objects/render texture/fireball.js
+++ ../public/src/es5/game objects/render texture/fireball.js
@@ -13,8 +13,9 @@
 
 var rt;
 var fireball;
 var fireFX;
+var player;
 
 function preload()
 {
     this.load.image('dude', 'assets/sprites/phaser-dude.png');
@@ -31,9 +32,9 @@
 
     fireFX = this.tweens.add({
         targets: fireball,
         scaleX: 3,
-        scaleY: 3,        
+        scaleY: 3,
         alpha: 0,
         duration: 300,
         ease: "Cubic.easeOut",
         onComplete: function () { rt.clear(); fireball.alpha = 0 },
@@ -55,12 +56,12 @@
     curve = new Phaser.Curves.Line(new Phaser.Math.Vector2(player.x, player.y), new Phaser.Math.Vector2(x, y));
 
     fireball.setPath(curve);
     fireball.startFollow(300);
-    
-    fireFX.restart();    
+
+    fireFX.restart();
 }
 
 function draw()
 {
\ No newline at end of file
     rt.draw(fireball);
-}
+}
